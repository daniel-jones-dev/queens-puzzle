[package]
name = "queens-puzzle-core"
version = "0.1.0"
edition = "2021"
description = "Core library for the Queens puzzle solver and generator"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
itertools = "0.14.0"
rand = "0.10.1"
log = "0.4"